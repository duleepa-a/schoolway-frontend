import VanOwnerDashboard from './VanownerDashboard';

// This is a Server Component (default in Next.js)
export default function VanOwnerPage() {
  // The page simply renders the client component
  return <VanOwnerDashboard />;
}